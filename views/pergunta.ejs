<%- include("layouts/header.ejs") %>

<body>
    <%- include("layouts/navbar.ejs") %>
    <div class="container">
        <div class="card">
            <hr>
            <div class="card-body">
                <h3><%= quests.titulo %></h3>
            </div>
        <div class="card-footer">
            <p><%= quests.descricao %></p>
        </div>

    <form method="POST" action="/responder">
    <textarea class="form-control" name="body" placeholder="Qual a resposta?"></textarea>
    </br>   
        <div class="card">
            <div class="card-body">
                <label>Respostas até o momento:</label>
                <% answers.forEach(answers => {%>
                    <ul>
                        <li><%= answers.body %></li>
                    </ul>
                <% })%>
            </div>
        </div>
    
        <input type="hidden" name="quests" value="<%= quests.id %>">
        <button type="submit" class="btn btn-primary">Responder</button>
        <a href="/" class="btn btn-primary">Voltar</a>
    </form>
    </div>
</body>

<%- include("layouts/footer.ejs") %>